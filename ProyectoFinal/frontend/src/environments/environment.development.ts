/**
 * Configuracion de entorno para desarrollo.
 * angular.json reemplaza environment.ts por este archivo en la configuracion "development".
 */
export const environment = {
  produccion: false,
  apiUrl: 'http://localhost:8080/api',
};
