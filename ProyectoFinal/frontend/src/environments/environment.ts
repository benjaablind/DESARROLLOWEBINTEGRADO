/**
 * Configuracion de entorno para produccion.
 * La API del backend Spring Boot expone todos sus recursos bajo /api.
 */
export const environment = {
  produccion: true,
  apiUrl: 'http://localhost:8080/api',
};
