package com.utp.odontologia.model;

/** Tipos de documento clinico que administra la clinica. */
public enum TipoArchivo {
    RADIOGRAFIA,
    FOTOGRAFIA,
    INFORME,
    CONSENTIMIENTO,
    RECETA,
    DOCUMENTO
}
