package com.utp.odontologia.model;

/** Estados por los que pasa un tratamiento, segun el plan de trabajo del equipo. */
public enum EstadoTratamiento {
    PENDIENTE,
    APROBADO,
    EN_PROCESO,
    COMPLETADO,
    CANCELADO
}
